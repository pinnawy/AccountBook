<UserControl x:Class="AccountBookWin.Welcome"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" KeyDown="UserControlKeyDown">
    <Grid>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" Margin="60" FontSize="50" Style="{DynamicResource TextBlock-Sketch}">Account Book</TextBlock>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock VerticalAlignment="Center" Style="{DynamicResource TextBlock-Sketch}" >Type Your UserName:</TextBlock>
            <TextBox Name="TxtUserName" Style="{DynamicResource BasicTextBox-Sketch}" Width="200" Margin="10,0,20,0" MaxLength="20"></TextBox>
            <Button Name="BtnGo" Style="{DynamicResource Button-Sketch}" Content="Go" Click="BtnGoClick"/>
        </StackPanel>
    </Grid>
</UserControl>
